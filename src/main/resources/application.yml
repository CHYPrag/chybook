spring:
  dataSource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bookstore?useSSl=false
    username: root
    password: "000000"
  redis:
    host: localhost
    port: 6379
    session:
      store-type: redis
      redis:
        flush-mode: IMMEDIATE
chybook:
  upload: # 从数据库查询到的都是相对路径，需要加上配置文件中前缀
    coverImg: "D:\\upload\\coverImg\\"# 图书封面图片上传存放路径
    chapterText: "D:\\upload\\chaptertext"# 图书每章文本资源路径
    chapterImg: "D:\\upload\\chapterImg"# 图书每章插图资源存放路径
    userImg: "D:\\upload\\user\\"#用户图像上传存放路径